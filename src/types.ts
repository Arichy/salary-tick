export type NativeSettingsJSON = {
  daily_salary: number;
  start_time: string;
  end_time: string;
  currency_symbol: string;
};

export type Settings = {
  dailySalary: number;
  startTime: string;
  endTime: string;
  currencySymbol: string;
};
